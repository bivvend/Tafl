<UserControl x:Class="Tafl.Views.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tafl.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
        <Grid Grid.RowSpan="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"></RowDefinition>
                <RowDefinition Height="1*"></RowDefinition>
                <RowDefinition Height="1*"></RowDefinition>
                <RowDefinition Height="1*"></RowDefinition>
                <RowDefinition Height="1*"></RowDefinition>
                <RowDefinition Height="1*"></RowDefinition>
            </Grid.RowDefinitions>
            <Button Grid.Column="0" Grid.Row="0" Command="{Binding NewBoardCommand}">New Board</Button>
            <StackPanel Grid.Column="0" Grid.Row="1">
                <CheckBox Name="CheckBoxAIAttacker" IsChecked="{Binding AttackerIsAI}">AI is Attacker?</CheckBox>
                <CheckBox Name="CheckBoxAIDefender" IsChecked="{Binding DefenderIsAI}">AI is Defender?</CheckBox>
            </StackPanel>
        <Label Name="LabelCurrentTurn" Grid.Row="2" Content="{Binding CurrentTurnState}"></Label>
    </Grid>
</UserControl>
